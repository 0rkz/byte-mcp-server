{
  "name": "@byte-protocol/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for Byte Protocol — AI agents can discover publishers, check reputation, subscribe to feeds",
  "type": "module",
  "license": "MIT",
  "main": "dist/index.js",
  "bin": {
    "byte-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "viem": "^2.21.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
