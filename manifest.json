{
  "manifest_version": "0.2",
  "name": "byte-mcp-server",
  "display_name": "BYTE Protocol",
  "version": "0.9.2",
  "description": "Per-byte USDC data marketplace for AI agents. Publishers slashed on-chain for bad data.",
  "long_description": "An MCP server for the BYTE Library data marketplace on Arbitrum. AI agents can discover data publishers, subscribe to feeds, monitor subscription health, query slashable fact-oracles, publish data, and buy single data packets via the x402 pay-per-call gateway — all settled in USDC. The nine read-only tools need no wallet; the four write tools and byte_buy_data (x402) require a private key.",
  "author": {
    "name": "0rkz",
    "url": "https://www.payperbyte.io"
  },
  "homepage": "https://www.payperbyte.io",
  "documentation": "https://github.com/0rkz/byte-mcp-server#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/0rkz/byte-mcp-server"
  },
  "license": "MIT",
  "keywords": ["mcp", "byte-protocol", "data-marketplace", "ai-agents", "arbitrum", "x402", "agent-commerce", "depin"],
  "server": {
    "type": "node",
    "entry_point": "dist/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/dist/index.js"],
      "env": {
        "PRIVATE_KEY": "${user_config.private_key}",
        "RPC_URL": "${user_config.rpc_url}",
        "INDEXER_URL": "${user_config.indexer_url}"
      }
    }
  },
  "user_config": {
    "private_key": {
      "type": "string",
      "title": "Wallet Private Key",
      "description": "Ethereum private key for write operations (subscribe, publish, faucet). Leave blank for read-only mode.",
      "sensitive": true,
      "required": false
    },
    "rpc_url": {
      "type": "string",
      "title": "Arbitrum RPC URL",
      "description": "Arbitrum Sepolia RPC endpoint. Uses a public default if blank.",
      "required": false,
      "default": "https://sepolia-rollup.arbitrum.io/rpc"
    },
    "indexer_url": {
      "type": "string",
      "title": "Indexer API URL",
      "description": "Byte Protocol indexer API URL. Uses the hosted default if blank.",
      "required": false,
      "default": "https://feeds.payperbyte.io"
    }
  },
  "compatibility": {
    "runtimes": {
      "node": ">=18.0.0"
    }
  }
}
